<%= link_to "Decks", decks_path %>
<h2><%= @deck.name %></h2>

<%= link_to "New card", new_deck_card_path(@deck) %>
<% @cards.each do |card| %>
	<div>
		<%= link_to card.front, [@deck, card] %>
	</div>
<% end %>